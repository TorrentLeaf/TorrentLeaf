# Screenshots

These captures are referenced directly by the root `README.md`, one per row at
full width. Keep the **exact filenames** below when replacing them. PNG
recommended, captured at a 1440×900 (desktop) viewport for the wide shots.

| Filename | What it shows |
| --- | --- |
| `Dashboard.png` | Library / dashboard (`/library`) — logged in, with torrents and the transfer chart. |
| `Reader-1.png` | Manga/comic reader — a page open. |
| `Reader-2.png` | Manga/comic reader — an alternate view. |
| `VideoPlayer.png` | Video player (`/watch`) — with controls / track selector visible. |
| `AddTorrent.png` | Add torrent page (`/add`) — the magnet input form. |
| `Settings.png` | Settings page (`/settings`) — reader defaults / download path. |

## Tips

- Keep file sizes reasonable (compress PNGs).
- For crisp text, capture at 1× (avoid 2× retina unless you also halve the display size).
- The README stacks each image full-width (one per row), so wide 16:9 captures look best.
